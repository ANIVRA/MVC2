@model IEnumerable<MVC2.Models.Post>

<div class="span3">
    <h4>Recent posts</h4>
    <ul class="link-list">

        @foreach (var item in Model)
        {
            <li><a href="#"><i class="fa fa-comment" />@Html.DisplayFor(modelItem => item.Title)</a></li>
        }

    </ul>
</div>